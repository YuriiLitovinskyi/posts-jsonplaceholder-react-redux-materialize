import React from 'react';

const About = () => {
    return (
        <div className="container">
            <h2>About this app</h2>
            <p>This is a simple front end React(Redux) application, that displays posts data from jsonplaceholder.</p>           
        </div>
    );
};

export default About;
